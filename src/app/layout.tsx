import type {Metadata,Viewport} from 'next';import {Cormorant_Garamond,Manrope} from 'next/font/google';import './globals.css';import {site} from '@/config/site';
const serif=Cormorant_Garamond({subsets:['latin'],variable:'--font-serif',weight:['400','500','600','700']});const sans=Manrope({subsets:['latin'],variable:'--font-sans'});
export const metadata:Metadata={title:'Kunduru Nagarjuna Reddy | Tax, Government Permissions & Property Services',description:'Professional assistance for tax services, government permission processes, property consultancy and interior design in Hyderabad, Telangana.',openGraph:{title:'Kunduru Nagarjuna Reddy',description:'Professional assistance for tax services, government permission processes, property consultancy and interior design in Hyderabad, Telangana.'}};export const viewport:Viewport={themeColor:'#0B0B0C'};
export default function Layout({children}:{children:React.ReactNode}){const schema={"@context":"https://schema.org","@type":"Person",name:site.name,email:site.email,address:site.address,jobTitle:site.positioning};return <html lang="en" className={`${serif.variable} ${sans.variable}`}><body><a href="#main" className="sr-only focus:not-sr-only focus fixed left-4 top-4 z-[100] bg-gold px-4 py-2 text-ink">Skip to content</a>{children}<script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/></body></html>}
