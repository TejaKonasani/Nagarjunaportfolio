# Kunduru Nagarjuna Reddy Portfolio

Install with `npm install`, develop with `npm run dev`, and create a production build with `npm run build && npm start`.

The portrait placeholder may be replaced at `public/images/profile/kunduru-nagarjuna-reddy.jpg` without changing the layout. The phone number is maintained centrally in `src/config/site.ts`.
